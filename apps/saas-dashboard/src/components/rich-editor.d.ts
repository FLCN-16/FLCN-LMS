import "quill/dist/quill.snow.css";
interface Props {
    name: string;
    defaultValue?: string;
    placeholder?: string;
    className?: string;
    minHeight?: number;
    mode?: "full" | "basic";
}
export declare function RichEditor({ name, defaultValue, placeholder, className, minHeight, mode, }: Props): import("react").JSX.Element;
export {};
//# sourceMappingURL=rich-editor.d.ts.map