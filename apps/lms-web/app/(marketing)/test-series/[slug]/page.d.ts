import { Metadata } from "next";
export declare function generateMetadata({ params, }: {
    params: Promise<{
        slug: string;
    }>;
}): Promise<Metadata>;
export default function TestSeriesDetailPage({ params, }: {
    params: Promise<{
        slug: string;
    }>;
}): Promise<import("react").JSX.Element | undefined>;
//# sourceMappingURL=page.d.ts.map